# Lina Decision Log
Use one entry for every decision that changes scope, design, architecture, content treatment or delivery date.

| ID | Date | Decision | Reason | Impact | Approved by | Status |
|---|---|---|---|---|---|---|
| D-001 | 2026-07-28 | Use one Master Product & Delivery Brief as source of truth | Prevent lost decisions and inconsistent work across ChatGPT and Claude | Creates controlled delivery workflow | Tebogo | Approved |
| D-002 | 2026-07-28 | Produce three directions, not three full products | Gives the client meaningful choice without wasting build capacity | Protects Friday deadline | Tebogo | Approved |
| D-003 | 2026-07-28 | Store all brand sources, exports and mock-ups inside the project asset folders | Prevents scattered assets and supports reuse/handover | Requires consistent naming and asset register updates | Tebogo | Approved |
| D-004 | 2026-07-28 | Missing client content will use labelled placeholders and remain in the inputs register | Prevents content gaps from stopping production | Final replacement remains outstanding | Tebogo | Approved |
| D-005 | 2026-07-28 | Original client assets will be stored under `assets/source/`, while working and approved project outputs will be stored in their relevant production folders | Removes ambiguity between original client files and project-produced assets; originals must never be overwritten or mixed with produced work | Asset Register, Project Plan and ChatGPT↔Claude Workflow updated to reflect the `source/` vs `working/`/`approved/` split | Tebogo | Approved |
| D-006 | 2026-07-28 | Direction C (Integrated Growth Platform) is recommended over Directions A and B, built on a single shared design system (colours sampled from the real logo, editorial serif + humanist sans typography, no purple/blue/glassmorphism) | Only direction that visibly serves both customer journeys (catering enquiry, mobile-kitchen order) and the admin/dashboard requirements in one page structure; nothing in the confirmed assets argues for A or B instead | `docs/LINA-IMPLEMENTATION-SPECIFICATION.md` created; three direction docs and a comparison doc stored under `assets/mockups/working/` | Claude (design sprint) | **Proposed — pending Tebogo/client confirmation at the 29 July direction-approval milestone** |
| D-007 | 2026-07-28 | Of 12 supplied images in `assets/source/social/`, only 1 is a confirmed genuine Lina photo (chef in trailer); 8 depict an unrelated restaurant's fine-dining plating or an unrelated "Lina's Kitchen" wellness ad and were rejected from all design use; 1 opening-flyer graphic and 1 stove photo are deferred pending confirmation; all 11 raw video clips are unreviewed (no frame-extraction tool available in this environment) | Prevents misrepresenting the product with mismatched imagery and avoids using media without confirmed usage rights | Design directions use labelled placeholders instead; see Asset Register curation section and Client Inputs Register I-012 | Claude (design sprint) | Proposed — awaiting Tebogo confirmation |
| D-008 | 2026-07-28 | ffmpeg/ffprobe (Gyan.FFmpeg 8.1.2) installed via winget, local-only, for media inspection; all 11 raw video clips visually reviewed via generated contact sheets and confirmed genuine (`@CHEF_LINA_MOEK` watermark on every clip). Hero decision: because all 11 clips are portrait/vertical (1080×1920) with no landscape alternative, the hero is **video-led on mobile** (`VID_20260728_040731_672.mp4`, chef plating on the trailer counter) and **image-led on desktop** (the previously confirmed `instagram_1785242438335.png`), rather than forcing one asset to do both jobs | No tool existed beforehand; ffmpeg was the explicitly authorised install. The hybrid hero avoids stretching/letterboxing vertical footage into a fake widescreen shape | `docs/LINA-VIDEO-MEDIA-REVIEW.md` created; Asset Register updated with per-video classification and 9 derivatives under `assets/mockups/working/media/`; near-duplicate clip `VID_20260728_040939_436.mp4` rejected in favour of `040731_672`; trailer signage in `VID_20260728_040923_128.mp4` surfaces an unconfirmed phone/email lead, logged as Client Inputs Register I-014 | Claude (media review) | Approved — video review complete; hero decision proceeds into prototype |
| D-009 | 2026-07-28 | Direction C (Integrated Growth Platform) is treated as **provisionally approved for prototype production**, retaining Direction A's chef/trailer storytelling and editorial occasions presentation plus Direction B's detailed visual menu and persistent WhatsApp ordering action | Tebogo's instruction explicitly authorises building a client-reviewable prototype on this basis without waiting for the 29 July milestone, while keeping the distinction between "provisional" and "final client-approved" clear | A static, front-end-only prototype is now being built per `docs/LINA-IMPLEMENTATION-SPECIFICATION.md`; no backend/admin/deployment work is in scope | Tebogo | **Provisional — not yet finally client-approved; final approval still awaits Milestone 3** |
| D-010 | 2026-07-28 | The Direction C static front-end prototype (`assets/mockups/working/prototype/`) is complete and verified: 14-part page structure, interactive menu built from `menu-data.js` (transcribed from the PDF), WhatsApp action with an honest "not yet confirmed" fallback state (no number invented), progressive-enhancement `<noscript>` menu fallback, and a verified-information-only trust section. Corrected one integrity issue found during review: the same photo had been attached to two differently-named Plates items, overclaiming a specific match the visual review didn't support — now only the one plausible item keeps the photo, the other two use a placeholder/their own distinct image | Verification run via a local Node static server (`serve.js`, no dependencies) and a custom CDP driver script against headless Edge (no chromium-cli or Playwright available; ffmpeg's earlier authorisation didn't cover browser automation, so a zero-install driver was written instead) | Zero console errors, zero failed asset requests, zero horizontal overflow at 375/768/1440px, working menu modal/gallery lightbox/form validation-and-success/keyboard focus, confirmed via `assets/mockups/working/client-review/*.png` screenshots | Claude (prototype build) | **Prototype build complete — still provisional; this is a client-review artifact, not a final client-approved deliverable** |
| D-011 | 2026-07-28 | **Prototype V1's visual execution is rejected** by Tebogo: the yellow-reading pale background, card-based menu/gallery layout, and long single-page slide-like structure are not approved. This is a rejection of the *visual execution only* — Direction C's underlying strategic combination (serving both the mobile-kitchen order journey and the private-catering enquiry journey from one platform, with admin-field alignment) is **not automatically discarded** and remains under consideration. **Direction D — Cinematic Catering Gateway** is now the active design exploration: a one-screen, full-bleed media landing gateway (no cards, no boxed images, dark warm palette with no dominant pale/yellow background) routing into five separate destination pages (Catering, Menu, Chef Lina, Mobile Kitchen, Gallery) plus a Contact/quotation page. Ballena Cabo (`ballenacabo.com`) was used strictly as a creative benchmark for design *principles* (full-bleed media, restrained typography, editorial pacing) — no layout, copy, code, colour, or branding was copied from it | Direct, specific client feedback naming the exact visual problems (yellow background, boxed/margined imagery, slide-deck feel, one-screen requirement, video underuse) | `assets/mockups/working/prototype-v2/` rebuilt around Direction D; V1 preserved untouched at `assets/mockups/working/prototype/` for comparison; both reachable via the local server (`/` for V1, `/v2` for V2); full rationale in `docs/LINA-PROTOTYPE-V2-DESIGN-RATIONALE.md` | Claude (design rejection + Direction D build) | **Direction D is a design exploration only — not approved by Tebogo, not approved by Lina. V1's visual execution is rejected; V1's underlying strategic scope is not** |
| D-012 | 2026-07-29 | **Tebogo approved the overall Direction D trajectory** and confirmed it is now much closer to what Lina's should be. Mobile experience specifically was positively received. Desktop required refinement, which was carried out as a **luxury/upmarket refinement pass**: (1) the dark colour palette was corrected from a measured warm/maroon-leaning near-black (`#16110E`, an 8-point R-over-B skew) to a neutral true-black (`#0A0A0B`, R/G/B within 1 point), with a restrained muted-bronze accent (`#B8996A`) added for decorative use only (dividers, hover states — never body text); (2) the landing hero was rebuilt as a sequence — the confirmed chef still image leads with a slow Ken Burns zoom, then crossfades (1.6s) into food-preparation video footage, with a staggered restrained text-reveal on load; (3) desktop type/spacing was scaled up significantly (hero headline up to 104px at ≥1280px) so large screens read as intentional rather than a stretched mobile layout; (4) the text-legibility scrim was strengthened and reshaped (layered radial + linear) specifically behind the text zone. **Luxury/upscale positioning (private dining, premium events, corporate clients) is now an explicit design priority**, not just a technical/functional one | Direct client feedback naming the exact desktop shortfalls (maroon-tinted black, insufficiently premium desktop hero, request for a cinematic still→video opening sequence) | During verification, a genuine defect was found and fixed: the hero video's original 2–21s trim included a ~1.5s window (~5–6.5s into the clip) where an Instagram music-attribution sticker overlapped the hero headline — re-trimmed to 9–21s of the same source clip, which is sticker-free and leads into a stronger "presenting the finished plate" closing moment. Verified via headless-browser automation with `prefers-reduced-motion` explicitly forced both ways: normal-motion shows the full still→video sequence correctly (crossfade confirmed completing ~3–4.6s in); reduced-motion never activates the video and shows the still only. Zero console errors, zero failed requests, zero horizontal overflow at 1280/1440/1600/1920/390px | Claude (luxury refinement pass) | **Refinement complete pending Tebogo's next review — not yet finally approved. This is a refinement of Direction D, not a new direction; V1 remains preserved and untouched** |
| D-013 | 2026-07-30 | **Structural tightening + disciplined brand-colour pass**, per Tebogo's follow-up review: the truer black from D-012 was confirmed as an improvement, but the design still read as "all over the show," and the interface was ordered narrowed to three colours only — solid black, solid red, solid white — with grey/maroon-black/bronze/muddy transitional tones removed entirely (the bronze accent added in D-012 is reversed here; food imagery naturally supplies additional colour and is exempt). Concretely: (1) every colour token rewritten to `--black`/`--white`/`--red` plus pure-opacity variants of black/white for secondary hierarchy — no new hues; (2) diagnosed and fixed the actual cause of "spread out": the hero content used 96px side padding while the header/sections used 24px, so nothing lined up — a single shared `--edge` variable now aligns the header logo, every hero, and every section to one vertical line (numerically confirmed: 0px difference between header-logo-left and hero-headline-left at 1440px); (3) the full-screen centred nav overlay (52–104px text spanning the whole viewport) was replaced with a controlled side-reveal drawer (400px / min(400px,86vw), right-anchored, backdrop-dimmed, opened by a plain hamburger icon instead of a text "MENU" button); (4) the redundant horizontal chapter-link row inside the landing hero was removed — the drawer is now the single navigation surface, not a second one; (5) menu items with no confirmed photo now show confident large-serif typography on black instead of a grey "Photo pending" apology, and items using a plausible-but-unconfirmed photo now carry a visible "Representative photo" tag directly on the tile, not just in the register — implementing "use representative imagery honestly rather than pretending it is the exact dish" literally in the UI | Direct client feedback: the three-colour discipline, the "all over the show" structural complaint, and the explicit request to explore a side-nav/drawer pattern | Verified via headless-browser automation: computed `body` background is exactly `rgb(0,0,0)`; header-logo/hero-headline left-edge difference measured at 0px; nav drawer measured at 400px width against a 1440px viewport (controlled side panel, not full-screen); 2 "Representative photo" tags and 1 typographic no-photo tile confirmed rendering on the Plates/Kota views; menu modal, gallery click-to-play video, WhatsApp honest fallback, and reduced-motion fallback all re-confirmed working; zero console errors, zero failed requests, zero horizontal overflow at 1280/1440/1600/1920/390px | Claude (structural tightening pass) | **Refinement complete pending Tebogo's next review — not yet finally approved. Still a refinement of Direction D; V1 remains preserved and untouched** |
| D-014 | 2026-07-29 | **Built one working end-to-end business journey** for the client meeting: catering enquiry → stored in a real local database → visible in an admin lead view → status updated + follow-up note added → dashboard figures update — ahead of, and separate from, the ongoing visual-direction work. No production backend existed, so per the stated preference order a lightweight local demo API plus file-backed persistence was built: Node's built-in `node:sqlite` module (zero npm installs, zero external services), a new `scripts/serve-demo.js` (does not modify or replace `scripts/serve-prototype.js` — `npm run prototype` is untouched), and new pages under `assets/mockups/working/demo/` (enquiry form, admin lead inbox, dashboard, customer/business journey explainer, progress report, one flyer concept, a 2-3 month roadmap preview, and a `/demo` hub). Reused the existing Direction D black/white/red token system — no new visual direction was introduced | Explicit instruction to demonstrate "a digital business platform, not only a website" for tomorrow's client meeting, without derailing the visual-refinement or full-roadmap work already scheduled | Full 11-step scenario verified via headless-browser automation through the actual UI (not just the API): enquiry submitted → reference number shown → appears in admin → New→Contacted with note → Contacted→Quoted → dashboard reflects the change → record and updates survive a full page reload. Zero console errors, zero failed requests. Test record deleted afterward so the database starts empty for the real meeting. Dashboard's R350,000 growth-goal progress and "average response time" are both honestly shown as 0%/placeholder rather than estimated, since no real revenue or elapsed-time data exists yet | Claude (working-model demo build) | **Working demo complete and verified — local review environment only, not production-secure, not deployed. Scope explicitly held to this one journey; production auth/CRM/payments/full roadmap/hosting remain out of scope per instruction** |

| D-015 | 2026-07-30 | **Built the Firebase-backed production enquiry/admin/dashboard platform (Phase 1)** on `feat/firebase-admin-platform`, replacing the local SQLite demo: Firestore data model (enquiries/enquiryActivities/adminUsers/settings), a validated Vercel serverless endpoint (`/api/enquiries/create`) as the sole enquiry writer (chosen over direct client Firestore writes — rules alone can't rate-limit, honeypot-check or dedupe), Firestore Security Rules with role-based access (owner/staff via custom claims), `/admin` portal (login, inbox, detail panel, owner-only dashboard), a controlled admin-bootstrap script (no public signup), and POPIA consent/timestamp tracking. `.vercelignore` extended to deploy `/api` and `/admin` while keeping Firebase project config and internal docs out of the Vercel build | Explicit instruction to convert the demo into "Phase 1 of the live Lina Digital Business Platform" — a form showing success without confirmed storage was called out as a critical failure to avoid | Verified twice: against the Firebase Emulator Suite (22/22 tests, 2 real bugs found/fixed) and again against real production Firebase + a Vercel preview (found/fixed a missing composite index, a fragile env-var parser, and unclear PEM error messages) | Claude | **Preview deployed and verified; PR unmerged, production untouched pending Tebogo's review** |
| D-016 | 2026-07-30 | **Reworked the enquiry notification workflow** after manual testing raised ambiguity about duplicate handling and email status: (1) replaced the 5-minute phone-only duplicate rule with a client-generated `submissionId` (exact-match idempotency) plus a narrow 30-second identical-payload fallback, so a repeat customer is never wrongly suppressed; (2) expanded email status from pending/sent/failed to pending/accepted/delivered/delayed/bounced/failed/suppressed with per-event timestamps, since "sent" only ever meant Resend accepted the API call, not inbox delivery; (3) split owner-notification and customer-confirmation into fully independent tracked emails (email became a required field); (4) added a signature-verified Resend webhook (`api/webhooks/resend.js`) to receive real delivery events; (5) admin inbox moved from one-shot reads to a live Firestore listener with a connection-state indicator; (6) added a customer-controlled "Save reference on WhatsApp" button using `wa.me` with no destination number, since Lina's WhatsApp number is still unconfirmed (I-006/I-014) | Direct instruction following ambiguous/possibly-defective manual test results; diagnosis found the actual root cause was that the tester's submissions never reached any deployment (no matching Firestore docs or server logs in the reported window) — not a real duplicate-suppression bug, though the underlying dedup logic was still broadened as requested | 16/16 Firestore rules tests, 12/12 unit tests, 0 production vulnerabilities, verified against real Firebase + Resend on a fresh preview. One real gap found: customer confirmation emails currently fail for any address other than the Resend account's own verified email, because the sender is still on Resend's sandbox address — needs a verified domain before real customers can receive confirmations | Claude | **Implemented and preview-verified; Resend webhook dashboard setup and domain verification still needed from Tebogo before this is complete; PR unmerged, production untouched** |

| D-017 | 2026-07-30 | **Replaced Resend with SMTP (Nodemailer via EliteHost)** for both operational emails, per explicit instruction: new `api/_lib/mail.js` (plain CommonJS, matching every other `api/_lib/*.js` module — no `src/` directory or TypeScript exists in this repo, so the originally-specified `src/lib/mail.ts` path was adapted), `resend`/`svix` removed from `package.json`. `api/_lib/send-notification-email.js` now builds the same owner/customer email content but sends via `mail.js` instead. Since SMTP has no delivery-status-webhook equivalent, `api/webhooks/resend.js` (D-016) was deleted and the admin panel's notification-status model was simplified from the 7-state pending/accepted/delivered/delayed/bounced/failed/suppressed set down to pending/accepted/failed — the richer states were only ever reachable via a Resend webhook that no longer exists | Direct instruction to swap providers; the two follow-on changes (deleting the webhook, simplifying admin statuses) were raised as a genuine architectural consequence and explicitly approved before being made, since leaving them would have meant dead code and permanently-unreachable UI states | `npm run admin:check`, unit tests (12/12) and Firestore rules tests (16/16) all still pass; `vercel build` (this repo has no `npm run build` — it's zero-build-step by design) completes cleanly with the new dependency graph; 0 production vulnerabilities. **Not yet tested with real SMTP credentials** — needs real `SMTP_*` values locally and in Vercel before an actual send can be verified | Claude | **Implemented, cannot be verified end-to-end until real SMTP_* values are supplied; PR unmerged, production untouched** |

| D-018 | 2026-07-30 | **Pivoted the admin-panel build from kitchen-operations to sales-and-marketing**, per explicit authoritative priority change, before the kitchen-ops work (a partially-implemented `bookings`/`tasks` collection design) was committed. Audited the in-progress work: `bookings` was simplified and repurposed as the master calendar's confirmed/tentative customer-event store (dropping `preparationStatus`, `checklistGenerated`, and non-customer event types); the general-purpose `tasks` collection/rules/tests were removed entirely (replaced by deriving follow-up reminders directly from `enquiries.followUpDate`/`quotations.followUpDate`/`contentItems.scheduledDate` — no second collection to keep in sync). Built: a shared 9-item sidebar/mobile-drawer (`admin/js/layout.js`) replacing per-page duplicated headers; an Overview command centre with owner/staff-scoped KPIs computed live from real data; a Sales Pipeline (stage view over the same enquiry documents, no duplicate lead records); one master Calendar merging bookings + enquiry follow-ups + quotation follow-ups + content schedule at render time (no separate `calendarEvents` collection); a Marketing Centre (content planner + campaign tracker with performance calculated live from linked enquiries, never stored as an independent total); Quotations and Invoices (both owner-only financial records, linked to enquiries); Reports; and a Settings page wiring the R350,000 goal to a real Firestore field it was previously hardcoded against. Enquiries extended with `campaignId/campaignName/campaignCode/utmSource/utmMedium/utmCampaign/landingPage/referralPartner/estimatedValue/quotationDate` (UTM/campaign values captured automatically from the public form's URL, never typed by staff), and a `source` enum expanded to the approved 9-value list. Added `isTestRecord` so dashboard/report figures never count QA data — retroactively flagged 13 of 14 real Firestore enquiries as test records (all identifiable by name markers from this engagement's own E2E testing), deliberately leaving the one ambiguous non-matching record ("Tebogo client") untouched rather than guessing | Direct instruction: the platform's immediate business need is sales/marketing capture and conversion, not kitchen-operations planning, which is deferred to Phase 2 in full (inventory, stock, suppliers, ingredient planning, equipment checklists, staff scheduling, transport/logistics, kitchen-production planning, automated prep checklists) | Also fixed the Firestore rules-test suite's real root cause of intermittent failures (not a rules bug — a Windows-only bug in `run-with-emulator.js` where `child.kill()` only killed the shell wrapper, not the actual emulator process tree, letting stale data leak across supposedly-independent test runs; fixed via `taskkill /T /F` plus `testEnv.clearFirestore()` in a `beforeEach` hook). 28/28 rules tests passing 3 consecutive times, 14/14 unit tests, 0 production vulnerabilities. E2E-verified against real Firebase on a fresh preview: sidebar/drawer on all 9 pages, pipeline stage changes, calendar category filtering, content-item creation appearing on the calendar, campaign-to-enquiry metric matching, quotation/invoice creation with atomically-generated reference numbers (found and fixed a real bug here — the original two-step create-then-update pattern could leave the number stuck at "PENDING" if the second write failed, as it did once under an artificially rapid 8-page test-navigation sequence; fixed by allocating the doc ID client-side and writing the real number in one `setDoc`), owner/staff/unauthenticated access boundaries. All synthetic test records created during this verification were deleted immediately after use; one incidental side-effect on the real "Tebogo client" enquiry (a status change and a quotation triggered by early rapid-navigation testing) was identified and reverted | Claude | **Implemented and preview-verified. PR #2 already merged in a prior round — this round's commits sit on `feat/firebase-admin-platform` unmerged and will need a new PR. Production untouched.** Verified Preview for commit `e58d363`: https://linas-cizm67k67-apprigate.vercel.app |

| D-019 | 2026-07-31 | **Documented the basis for the `isTestRecord` classification of the 14 production enquiries** (D-018 flagged 13 of them). LINA-20260730-0001 through 0013 were marked `isTestRecord: true` because their customer names match name markers produced only by this engagement's own automated/manual QA (E2E, "R2 ", "Verification Test"). LINA-20260730-0014 ("Tebogo client") was deliberately left unclassified because it carries no such marker and its origin is genuinely ambiguous — it was not guessed at either way. No `enquiryActivities` entry records the classification change on any of the 13 records, because the flagging was performed via a one-off Admin SDK maintenance script (bypasses Firestore rules and the client-side activity-logging code) rather than through the app. No records were reclassified or deleted by this entry — it is documentation of a decision already made in D-018 | A release-readiness audit asked for the classification evidence to be recorded in writing rather than left implicit in D-018's narrative, and flagged the missing audit trail as a gap worth naming explicitly | None — no Firestore writes. This is a documentation-only entry | Claude | **Recorded. Risk noted: classification was by name-pattern match, not a stored provenance field, so there is residual (low) risk a genuine enquiry was misclassified — Lina should confirm during her first real review. See Project Plan Phase 2 backlog for the proposed fix (server-generated provenance marker at creation time), not implemented in this release** |

| D-020 | 2026-07-31 | **Public site consolidated to a single Version 2 experience at clean URLs; admin platform visually restructured with owner/developer/observer roles.** Public: `vercel.json` changed from a redirect (which exposed `/assets/mockups/working/prototype-v2/...` in the browser's URL bar) to rewrites, so `/`, `/catering`, `/menu`, `/chef-lina`, `/mobile-kitchen`, `/gallery`, `/contact` are now the real, clean, indexed URLs; every internal link/asset reference across all 7 pages converted from relative to absolute paths to support this. Removed the `/v2` route entirely. All internal review commentary (`prototype-banner` divs, "design-review prototype" footers, `(prototype v2)` title suffixes, `Client Inputs Register I-XXX` citations in footers/captions, and two developer-only justification notes on the Chef Lina and Mobile Kitchen pages) stripped from every page — while deliberately preserving the underlying honest-disclosure behaviour those notes protected (the WhatsApp button still shows a real "not connected yet" message, just without the internal ticket reference; the "Representative photo" tag stays, since that one is genuine customer-facing information, not internal commentary). V1 (`assets/mockups/working/prototype/`), the local demo, and both client-review screenshot folders were never part of the Vercel deployment to begin with (`.vercelignore`'s allowlist never admitted them) — confirmed via dependency check before treating this as done; nothing was deleted, they remain in Git history and on disk for reference. Admin: rebuilt the sidebar visibly (280px, solid black, red active-state background+border, editorial serif nav labels, grouped Sales/Marketing/System headers, prominent "Lina's / Digital Business Platform" lockup, Escape-key drawer support) — the previous sidebar was functionally present but read as generic. Added `developer` (full system access, functionally identical to owner) and `observer` (read-only across every module except Users/Settings) roles throughout `firestore.rules` (enforced server-side, not just hidden buttons — see the 4 new rules tests) and every admin page. Rebuilt Overview per the new brief: greeting/date/status-statement header, exactly 6 KPI tiles (New leads, Pipeline value, Revenue won, Conversion rate, Overdue follow-ups, Upcoming bookings), 5 dependency-free SVG-free chart primitives (`admin/js/charts.js` — horizontal bars for the funnel/leads-by-source/campaign-performance, vertical bars for revenue-over-time, all black/red/white with restrained opacity steps, honest empty states, no fabricated trends), and exactly 4 compact (≤5-item) panels each linking to its full module — replacing the previous Overview's 8 sprawling full-module-like panel lists. Added a Users module (`admin/users.html`/`users.js`) reading Firebase Auth + `adminUsers`, plus a real (but never self-triggering) invitation endpoint (`api/admin/invite-user.js`) that creates/updates a Firebase Auth account, sets its role claim, writes its `adminUsers` doc, and returns a password-SET link for a human to share manually — it never emails anyone itself. Added a discreet, session-shared "Admin" link on the public site (`public-admin-link.js`, invisible to anonymous visitors, visible only after a real signed-in admin role is confirmed) and a "View Public Site" sidebar item, both verified on the live Preview to share one Firebase Auth session with zero re-login | Explicit authoritative decisions: the client approved Version 2 as the public site, admin needed a visible (not just technical) redesign, and Lebo/Rebecca/Tebogo needed correctly-scoped Owner/Observer/Developer accounts | 32/32 rules tests (4 new: developer full-access parity, observer read-succeeds/write-denied across every business collection, observer denied settings/users, self-login-timestamp update) passing 3 consecutive times; 14/14 unit tests; `admin:check` clean across 27 files; 0 production vulnerabilities. A real bug was found and fixed during verification: `menu-data.js` held 2 relative `../media/...` image paths the initial absolute-path conversion missed, which 404'd under the new clean-URL routes — fixed and reconfirmed with zero failed requests/console errors on a second, final Preview deploy. Full QA pass on that final Preview via CDP automation: clean public/mobile screenshots, all 6 clean routes resolving with real content, zero console errors, zero failed requests, sidebar/charts/Users module all rendering correctly for the real owner account, mobile drawer + Escape verified, and the public→admin→public session bridge round-tripped with no re-login required. **Not verified on the live Preview:** the new `developer`/`observer` roles' actual behaviour against real Firestore — deploying the updated `firestore.rules` to the real `lina-s` project (shared by both Preview and Production, since there is no separate preview Firestore) was blocked by the session's own permission classifier as a live security-config change, so it was left for explicit human approval rather than routed around; the rules logic itself is fully proven correct via the emulator suite above, independent of this | Claude | **Public-site and admin-UI work implemented and preview-verified (fresh Preview: see report). Firestore rules deployment to the real project — required before developer/observer roles work end-to-end — awaits explicit approval. PR unmerged, Production untouched, no Firestore records touched.** |
| D-021 | 2026-07-31 | **Backend order records and sales tracking added.** The public cart ("Order via WhatsApp") now creates a server-validated order record in Firestore *before* WhatsApp opens, instead of only generating a message. Built: `api/_lib/menu-catalog.js` (server-side authoritative price list, integer cents, frozen ids) with a parity test that fails if `menu-data.js` and the catalogue ever drift; `api/_lib/validate-order.js` (prices every line from the catalogue — any client-supplied price/subtotal is read and discarded, never trusted); `api/orders/create.js` (rate-limited, idempotent on a required `submissionId`, writes the order and a `created` audit entry in one transaction, SAST-dated reference `LINA-ORD-YYYYMMDD-NNNN` — deliberately not sharing the enquiry endpoint's UTC-dated counter, since SA is UTC+2 and a shared/UTC counter would misdate or collide with late-night orders); a public two-step cart (`assets/mockups/working/prototype-v2/cart.js`, sessionStorage-only, ids+quantities never prices) whose second step renders a real, pre-rendered `<a href="wa.me/...">` from the server's response — a genuine tap, so unlike a `window.open()` after an `await` it cannot be mobile-popup-blocked; new Firestore rules for `orders`/`orderActivities` (create/delete both server-only, update restricted by a `hasOnly` allowlist, terminal-status branch checked before transition rules so editing a note on a Collected order isn't rejected as an invalid status change); and an admin Orders module (`admin/orders.html`/`orders.js`) with a Today board and an All-orders history, owner/developer write, observer read-only, staff excluded entirely (matching the brief's named roles). **Only the `Collected` status counts as a sale** — `Pending WhatsApp`/`Confirmed`/`Preparing`/`Ready for Collection` are real, tracked intent but never revenue; `Cancelled`/`Not Collected` require a typed reason and are also never revenue. Order revenue (`subtotalCents`, integer cents, `fmtCents` with decimals) is kept structurally separate from enquiry revenue (`confirmedAmount`, whole Rands, `fmtRand`) — different field names, different units, own dashboard panel, own Reports section, own CSV export, and the R350,000 growth-goal bar stays enquiry-only; `admin/js/order-constants.js` is the single shared source for the sale-status list and formatting so the three consuming pages (dashboard/reports/orders) cannot drift apart | Direct instruction: WhatsApp opening was previously indistinguishable from a real sale, so Lina had no way to separate genuine intent, abandoned browses, and actual collected revenue, and no order-level audit trail existed at all | `admin:check` clean across 35 files; 58/58 unit tests (`menu-catalog-parity`, `validate-order`, `order-message`, plus the pre-existing `validate-enquiry` suite); 51/51 Firestore rules tests passing 3 consecutive times; 0 production vulnerabilities (`npm audit --omit=dev`). **Verified against real production Firestore on a fresh Preview** (`https://linas-n4r1spheu-apprigate.vercel.app`), not just the emulator: `cart.js`/`menu-data.js`/`script.js` confirmed byte-identical between disk and the deployed Preview (the `.vercelignore` allowlist risk flagged in planning did not materialise); a live order created via direct API call proved a forged `subtotalCents`/`unitPriceCents` in the request body is fully ignored (server priced R32.00 from two catalogue items regardless); a repeated `submissionId` returned the original order rather than creating a duplicate; the 6-request-per-minute rate limit correctly triggered a 429; a full real-browser (headless Edge via CDP) run through the actual deployed UI — add-to-cart, two-step panel, POPIA consent, submit — created a real order with a real anchor tag WhatsApp link matching the exact stored message, zero console errors, zero failed requests; the order was then walked through every status to Collected via a maintenance script mirroring the admin UI's exact `writeBatch` shape, producing the correct 5-entry audit trail and correctly counting only the Collected order as a sale while two other still-open test orders correctly did not count. **A real gap was found and fixed during this verification, not by design:** the new `orders`/`orderActivities` Firestore rules — already proven correct via 51/51 emulator tests — had never been deployed to the live `lina-s` project (last live ruleset was the D-020 restructure), which would have silently denied the admin UI all order reads/writes in both Preview and Production, since they share one Firestore project. Confirmed via the Firebase Security Rules REST API before touching anything, then deployed with explicit authorization; re-confirmed immediately after that unauthenticated client reads and direct client-side creates are still correctly denied. All 4 real order documents created during this verification (`LINA-ORD-20260731-0001` through `0004`) were flagged `isTestRecord: true` via a one-off Admin SDK maintenance script — same pattern as D-019 — rather than deleted, since orders have `allow delete: if false` with no exceptions | Claude | **Implemented and verified against real production Firestore on a fresh Preview. Firestore rules deployed to `lina-s` (explicit approval obtained first). One follow-up gap: the composite Firestore indexes for `orders`/`orderActivities` (in `firestore.indexes.json`) were not deployed — blocked by the same session safeguard that gates live-infrastructure changes. This does not affect order creation, the Today board, or dashboard/report figures (none of those queries need a composite index); it will affect only the order-detail panel's activity-history lookup (`where(orderId==) + orderBy(createdAt)`), which will show a Firestore "requires an index" error until the index is created — either by re-running `firebase deploy --only firestore:indexes --project lina-s` with explicit approval, or via the auto-generated console link Firestore returns in that error. PR unmerged, Vercel Production untouched — the standing instruction was not to deploy Production until the full lifecycle was tested, which is now done** |
| D-022 | 2026-07-31 | **Live Production release of the backend order-tracking work (D-021), under explicit written release authorization naming commit `e55e6df`.** The composite Firestore indexes flagged as outstanding in D-021 were deployed (`firebase deploy --only firestore:indexes --project lina-s`) and confirmed `READY` on all 3 (`orders`: `orderDateKey`+`createdAt`, `orders`: `status`+`createdAt`, `orderActivities`: `orderId`+`createdAt`) within seconds, given the small collection size. `firestore.rules` was re-deployed for release confirmation (no content change — already live from D-021). Vercel Production was built and deployed from the exact `e55e6df` working tree (`vercel pull --environment=production`, `vercel build --prod`, `vercel deploy --prebuilt --prod`) and `linas.co.za` was confirmed to alias the new deployment (`dpl_3fv9XYdv2k7Zwfkz9tZ45khYeyUK`) via byte-identical content hashing against the deployment URL directly | Direct, explicit written release authorization covering indexes, rules confirmation, Production deployment, and a specific live test plan | **Full live verification on `linas.co.za` itself, not Preview:** public cart tested end-to-end via headless-browser automation (add/add-different/increase/decrease/remove/session-persists-across-refresh/review), producing real order `LINA-ORD-20260731-0005` with a real WhatsApp anchor and exact golden-format message; three temporary, clearly-named Firebase Auth test accounts (`release-test-{owner,developer,observer}-<timestamp>@linas-release-test.invalid`, RFC-2606 reserved non-routable domain, authenticated via server-generated custom tokens rather than real email/password so no email delivery was ever needed) were created, used to sign in live and walk the test order through the entire admin UI — not a maintenance script — from Pending WhatsApp through Confirmed/Preparing/Ready for Collection to Collected, confirming all 6 audit-trail entries (creation + 4 transitions + 1 internal note) render correctly with the now-`READY` index and zero console errors; confirmed owner and developer have full parity (save button, all fields editable) while observer sees the identical data fully read-only (no status/save/customer/payment/notes controls) and a direct client-side Firestore write attempt from the observer session was denied by rules (`permission-denied`); confirmed the order counted as a real sale (R60.00) on the live dashboard while enquiry revenue and the R350,000 goal bar stayed untouched, then confirmed the figure dropped to R0.00 immediately after the order was flagged `isTestRecord: true`; all 3 temporary test accounts were deleted immediately after use. Production smoke-tested at 1440/1280/768/390px: zero horizontal overflow, zero console errors on a fresh (never-authenticated) browser profile, reduced-motion correctly suppresses the hero video, the enquiry form and a "Login" link are both present and reachable. Regression suite re-run for the release: `admin:check` clean, 58/58 unit tests, 51/51 rules tests, 0 vulnerabilities. **One discrepancy surfaced and deliberately not fixed in this release:** the release checklist referenced confirming a "Pay online — Coming soon" label, but no such label exists anywhere in the shipped public cart/menu code — online payment was never built this round (explicitly out of scope per the original plan), so there was nothing to confirm. Per the release's own "no new functionality" constraint, no such label was added; flagged here for a deliberate future decision rather than silently built now | Claude | **Live on Production. `linas.co.za` confirmed serving `e55e6df`. All 5 real order documents created across this round's Preview and Production testing (`LINA-ORD-20260731-0001` through `0005`) are flagged `isTestRecord: true`, none deleted. GO — see the session's final release report for the full itemised result.** |

| D-023 | 2026-08-01 | **Coming Soon / private-preview gate built** on `feature/coming-soon-private-preview` (branched from the archived stable build, `archive/linas-live-before-poster-redesign-2026-08-01`, ahead of the poster-led redesign). Gates the 7 public routes (and their raw `.html` twins) behind a branded launch page while the enquiry endpoint, WhatsApp, and the entire admin platform keep working untouched. Built with `middleware.js` (Vercel Routing Middleware, Node runtime — Edge doesn't support the `crypto` module the signing code needs), a shared HMAC-signed, access-versioned cookie library (`api/_lib/preview-token.js`), and a pure route classifier (`api/_lib/preview-paths.js`) that explicitly protects both the clean URL and the raw `.html` path for every public page, closing the bypass a clean-URL-only gate would leave open. Three access paths: `api/preview/unlock.js` (shared password, constant-time compared, rate-limited, generic error on any failure), `api/preview/developer-unlock.js` (verifies the caller's real Firebase ID token server-side, allows only owner/developer — mirrors `invite-user.js`'s existing trust boundary exactly), and an "Unlock Website Preview" admin sidebar action. `COMING_SOON_ACCESS_VERSION` revokes every outstanding cookie instantly by bumping one env var, no code change. Countdown and promo regions exist but stay hidden until a real launch date/promo copy is configured — never a fabricated one. See `docs/LINA-COMING-SOON-MODE.md` for the full operations runbook | Direct instruction: the public site needed a premium branded holding page during the redesign while the business kept operating (enquiries, WhatsApp, admin) uninterrupted, with controlled private review access | 27 new unit tests for the token/path libraries (tamper, expiry, access-version mismatch, wrong-secret, every classification case including the raw-`.html` bypass) plus 5 for the countdown date parser — all passing; `admin:check` clean; 0 new vulnerabilities. **Verified live on a throwaway Preview** (env vars passed per-deployment via `vercel deploy -e`, never persisted to project settings — activation is explicitly deferred to a separate authorization): gate-off state confirmed byte-for-byte identical to today; gate-on confirmed blocking every protected route AND its raw `.html` twin (302, intended path preserved); a correct-password cookie passes `/menu` and `/` through with `Cache-Control: private, no-store`; a tampered cookie safely redirects rather than crashing or granting access; `/admin` and `/api` completely unaffected in both states; the real Coming Soon page enquiry form stores a real enquiry and returns a real reference; owner/developer/observer/staff/anonymous role gating on the developer-unlock endpoint all verified against the Auth emulator. **Two real bugs found and fixed during this verification, not by design:** (1) the hero image originally chosen (`lina-chef-trailer-frame`) had "PHONE: +27…" burned into the pixels — an unconfirmed contact number per Client Inputs I-014 — swapped for the frame D-008 already confirmed clean; (2) `.cs-modal-backdrop`'s unconditional `display:flex` had equal CSS specificity to the browser's `[hidden]` rule and won by cascade order, so the Private Preview modal rendered open on every page load regardless of state — fixed with an explicit `[hidden]` override, confirmed via computed-style check on a fresh page load. All test enquiries/orders created during verification flagged `isTestRecord: true`, none deleted | Claude | **Built and Preview-verified. `feature/coming-soon-private-preview` pushed, PR not yet opened. Neither Production nor the archive branch/tag touched. `COMING_SOON_ENABLED` and the real reviewer password/cookie secret are NOT configured anywhere yet — activation awaits a separate explicit instruction, per the brief** |

| D-024 | 2026-08-01 | **Coming Soon mode activated on live Production** (`linas.co.za`), under explicit written authorization naming commit `588083d`. Configured 5 Production environment variables (`COMING_SOON_ENABLED=true`, `COMING_SOON_ACCESS_VERSION=1`, `COMING_SOON_PROMO_ENABLED=false`, `COMING_SOON_COOKIE_SECRET` — freshly generated, 32 random bytes, never Preview-reused — and `COMING_SOON_REVIEWER_PASSWORD` — supplied directly by the project owner). `COMING_SOON_LAUNCH_AT` deliberately left unset; countdown stays hidden. Built and deployed the exact authorized commit from an isolated worktree (not the branch's later docs-only HEAD, to match the authorization literally) via `vercel pull --environment=production`, `vercel build --prod`, `vercel deploy --prebuilt --prod`; `linas.co.za` confirmed aliased to the new deployment | Direct, explicit written Production-activation authorization, following gate-enabled Preview verification (this session, prior round) that had already proven every code path | **Full live verification directly on `linas.co.za`:** all 6 protected routes plus their raw `.html` twins return 302 with empty redirect bodies (no hidden site content); admin and all APIs unaffected; homepage/tablet/mobile (1440/768/390) all render with zero console errors and zero failed requests; a real enquiry (`LINA-20260801-0004`) and a real order (`LINA-ORD-20260801-0003`) were created live during verification and immediately flagged `isTestRecord: true`; the real reviewer password correctly rejects a wrong attempt (generic error) and, when correct, redirects to the originally-requested route, persists across navigation and refresh, is a properly signed/HttpOnly/Secure/SameSite=Lax/7-day cookie invisible to JS, grants zero admin access, and Exit Private Preview correctly re-gates the site; owner/developer roles (tested via temporary, clearly-named accounts — no real account credentials were available, and listing live users is blocked by this session's own safety classifier) pass the developer-unlock endpoint and the real "Unlock Website Preview" admin-sidebar action, opening the actual site in a new tab while the admin session and "Back to Admin" path remain live; observer/staff/anonymous all correctly denied (403/403/401). `admin:check` clean, 90/90 unit tests, 51/51 rules tests (unchanged), 0 vulnerabilities. Previous Production deployment (`dpl_3fv9XYdv2k7Zwfkz9tZ45khYeyUK`) and the archived rollback branch/tag/bundle/zip all confirmed intact and untouched | Claude | **Live on Production. `linas.co.za` now serves the Coming Soon gate. GO — see the session's final activation report for the full itemised result. The reviewer password was relayed through this chat session at the project owner's explicit choice, after being offered two channels (Vercel dashboard, own terminal) that would have kept it fully outside this session's visibility** |

| D-025 | 2026-08-02 | **First poster-led luxury homepage direction built** on `feat/poster-led-luxury-refinement`, first-screen only — every existing backend/operational feature (Firebase, Auth, admin, dashboard, enquiry storage, cart, ordering, APIs, Firestore rules, WhatsApp, all 6 internal pages) left untouched. New `home-media.js` (central config — every image path, focal point, alt text and destination link in one file, matching the existing `menu-data.js` pattern), `home.css` (scoped under `body.home`/`.home-*`, loaded after `styles.css`), `home.js` (builds the 3 destination rows, wires desktop hover-reveal / mobile two-tap). `index.html`'s hero replaced with a warm-white, numbered editorial index (01 Explore Our Menu → `/menu`, 02 Order Today → `/mobile-kitchen`, 03 Plan an Event → `/catering`, folding Request a Quote into the third rather than competing as a fourth); *Meet Chef Lina* stays a nav-drawer item, not a fourth gateway. Four **deliberate divergences from the standing brand system**, each logged rather than silently applied: (1) **`#B2373E` used homepage-only** — `styles.css` keeps the documented `#A43129` for all 6 internal pages this round, so two brand reds now coexist by decision; needs a site-wide alignment call next round or it becomes drift, not divergence. (2) **Warm-white/red-led composition, black reserved for a lower accent band** — reverses D-011/D-013's black-led three-colour system; superseding for the homepage only, pending Lina's sign-off. (3) The approved logo file (`Linas_Logo_nb_wl.svg`, white line-art, transparent) is illegible on this page's white background as-is; given a small `#B2373E` backing plate in the header — same red already sampled from this exact logo file for the Coming Soon top bar (D-023's predecessor work), so no new colour is introduced and the logo graphic itself is untouched (same file, same proportions, nothing redesigned/rearranged/separated). (4) The bare icon-only hamburger becomes a labelled "MENU" text control on this page only (internal pages keep the icon) — same `#navToggle` id, same `script.js` click handler/drawer/focus-trap/Escape logic, only the button's own markup/styling changed. **Chef Lina, not "Chef Lebo"** confirmed as the correct public-facing name (matches trailer signage, uniform embroidery, `@CHEF_LINA_MOEK`, `/chef-lina`) — Lebo remains the admin account owner (I-016) only, never a public identity. **No video on the first screen**: every available clip is portrait-only with no landscape source (D-008 already settled this) and carries a burned-in Instagram watermark in every frame — both hard constraints, not a style preference; the existing proven still-hero sequence (`script.js:60-85`) is left completely alone and unused this round rather than forced below an unbuilt fold. **`Green-plate.png` — the single most premium-looking image in the new 2026-08-02 media batch — found to be a background-removed copy of `instagram_1785244457096.png`, an image already Rejected under I-012 with unconfirmed usage rights.** Excluded from every slot; the batch's other 8 images are used as usable-pending-confirmation (new Client Inputs Register item I-018), not treated as cleared | Continuation of the poster-led luxury homepage refinement brief: create the first design direction for the opening viewport only, chef-led/premium/editorial, while proving out zero regression to everything already shipped | `npm run admin:check` clean, 90/90 unit tests, 51/51 Firestore rules tests, 0 vulnerabilities — all unchanged from the pre-task baseline, confirming nothing outside the homepage first screen was touched. Verified in a real headless Chromium browser (`playwright-core`, driven against an already-cached local browser binary, installed `--no-save` for this session only — not added to `package.json`) at 1440/1280/768/390px: zero horizontal overflow, zero console errors, first screen fits exactly within `100svh`/`100dvh` at every width with no scroll needed. Keyboard Tab reaches logo → MENU → all 3 destinations → scroll cue with a visible focus ring at each stop; MENU opens the existing drawer via Enter, Escape closes it and returns focus to the trigger — confirming `script.js`'s drawer logic works unmodified under the new labelled control. Real touch emulation (`hasTouch`/`isMobile`) confirmed the mobile two-tap contract exactly as specified: first tap expands a row in place (image + blurb reveal, `aria-expanded` flips) with no navigation, a second tap on the same row navigates. Desktop pointer/focus hover confirmed the dish-cutout crossfade and the active-row red underline draw. Screenshots: `assets/mockups/working/client-review-poster-led/*.png`. One implementation refinement from the approved plan, noted for transparency: the plan's slot table reserved a separate `hero-dish` slot (`Rice.png`) as a resting-state image distinct from the three destination-preview images; since the interaction spec already defaults to destination 01 active on load, a fourth distinct resting image would never be visually distinguishable from destination 01's own image, so it was not wired in — `Rice.png`'s WebP derivative was still produced and is held in reserve (see Asset Register) rather than left unbuilt | Claude | **Preview only, as instructed. No commit, push, merge, deploy, alias change or Coming Soon interaction performed. Awaiting explicit approval of the visual direction before anything is committed** |

| D-026 | 2026-08-02 | **Poster-led homepage rebuilt as a split editorial hero, superseding D-025's numbered-index direction** on the same `feat/poster-led-luxury-refinement` branch, under explicit instruction naming a reference image and era-residence.com as an interaction/presentation reference only. Same three files (`home-media.js`, `home.css`, `home.js`) and `index.html`'s first screen rewritten in place; D-025's destination hover-reveal/crossfade system (numbered 01/02/03 index, per-destination dish-image crossfade, mobile two-tap accordion) is fully removed in favour of a static composition, per explicit instruction to prioritise "exact visual direction, composition, typography, spacing, hierarchy" over interaction this pass. New structure: white header with a horizontal desktop nav (Home/Catering/Menu/Meet Chef Lina/Find Us) + a "Request a Quote" button, both new and homepage-only; the existing labelled MENU control/drawer trigger (`#navToggle`) is kept at every width specifically so Gallery and the authenticated-admin "Back to Admin" link (`public-admin-link.js` → `#navLogin`, only reachable via the drawer) remain reachable on desktop, since the brief's reference nav omits both — not an oversight, a deliberate preservation of an existing working path. Split hero body: editorial copy (tagline, "Lina's" eyebrow, large red serif headline, supporting paragraph, two CTAs — Request a Quote primary, Explore the Menu outline) on the left, the confirmed chef photo on the right; a slim red service ribbon ("Freshly prepared meals. · Private catering. · Mobile kitchen.") closes the first screen above a minimal scroll cue. The three dish-crossfade WebP derivatives built for D-025 (`lina-dish-steak-pap`/`tripe-dumpling`/`catering-bowl`) are no longer referenced by any page; their `.vercelignore` allowlist lines were removed so nothing unused deploys, but the files themselves and their Asset Register rows are left in place (still legitimate, reusable dish photography) rather than deleted. **No true chef cutout exists anywhere in the project** — confirmed by a fresh search of both `assets/source/media/` and `assets/mockups/working/media/` before starting; the only other chef-identifying image (`lina-chef-trailer-frame`) remains excluded per D-023 (burned-in phone number). Rather than fabricate a cutout (no background-removal capability is available in this environment — the same blocker raised and accepted earlier this session for the unrelated food-cutout request), the real confirmed photo (`lina-hero-poster-working-v1-20260728.jpg` — chef at the trailer counter, genuine trailer interior background, plated meal, "CHEF LINA" signage visible) is used directly as a bordered photographic panel, not presented as an isolated cutout. All D-025 divergences carry forward unchanged and are not re-litigated here: homepage-only `#B2373E`, white/red-led composition reversing D-011/D-013, the logo's red backing plate (same red already sampled from this exact logo file for the Coming Soon top bar), and the `Green-plate.png` exclusion (I-018 still open) | Direct, explicit instruction providing a reference image and naming era-residence.com for interaction/presentation tone only ("do not copy literally"), stating the previous direction did not match the brief closely enough and must be replaced, not iterated on incrementally | `npm run admin:check` clean, 90/90 unit tests, 51/51 rules tests, 0 vulnerabilities — all unchanged from the pre-task baseline, re-confirmed after the rebuild. Re-verified in real headless Chromium at 1440/1280/768/390px: zero horizontal overflow, zero console errors, first screen fits exactly within `100svh`/`100dvh` at every width. Keyboard Tab now correctly walks logo → Home/Catering/Menu/Meet Chef Lina/Find Us → header Request a Quote → MENU → hero Request a Quote, a visible focus ring at every stop; MENU still opens the existing drawer via Enter, Escape still closes it and returns focus — confirming `script.js`'s drawer logic is fully intact under the new header markup. `prefers-reduced-motion: reduce` confirmed collapsing the new fade-in reveal to fully opaque/no-transform on load (computed opacity 1), via the existing global rule in `styles.css` — no new reduced-motion handling needed. One real layout bug found and fixed during this pass: the red ribbon's separator dot could wrap onto its own line at mobile widths, orphaned from both neighbouring labels; fixed by moving the separator to a `::before` on each label (CSS) instead of a sibling element, so dot and label can never be split by wrapping — confirmed fixed via a fresh mobile screenshot. Screenshots: `assets/mockups/working/client-review-poster-led/v2-*.png` (replacing the D-025 screenshot set, which documents the superseded direction and is left in place for reference) | Claude | **Preview only, as instructed. No commit, push, merge, deploy, alias change or Coming Soon interaction performed. Awaiting explicit approval of this visual direction** |

| D-027 | 2026-08-02 | **Chef Lina's background removed from the homepage hero image, replacing the D-026 full-photo panel with a genuine cutout.** The client supplied `Lina's_Welcome.png` directly to `assets/source/media/client-latest-2026-08-02/processed-transparent/` — a background-removed version of the confirmed trailer-counter photo (1440×1920, RGBA, colour type 6). Verified genuine alpha before use, same method as the D-025 dish cutouts: decoded IHDR/IDAT and confirmed alpha=0 at the sky/background sample points and alpha=255 over the chef/counter, not just trusting the colour type. Unlike the dish cutouts, this is not a fully isolated silhouette — only the trailer's upper interior (shelving, water bottles) is removed; the counter, plated meal and red "Lina's / CHEF LINA" signage stay attached as one unit, which reads as an intentional grounding element rather than a defect. Produced one web derivative, `lina-hero-chef-cutout-working-v1-20260802.webp` (900×1200, `yuva420p`, ~73KB, via `ffmpeg -vf scale=900:-1 -c:v libwebp`), added to `.vercelignore`; the source PNG is untouched. `home-media.js`'s `heroImage` repointed at the new file; `home.css`'s `.home-hero__visual` rebuilt from a bordered/cropped photo panel to an unbordered cutout, bottom-aligned so the chef and her counter sit on the same visual baseline as the ribbon below — a boxed border around a transparent image would have drawn a visible rectangle around empty space, so the panel treatment from D-026 was removed rather than kept. One tooling note: `ffmpeg.exe` (a native Windows binary invoked from Git Bash) failed to open the source path with a plain `/c/...` Unix-style path containing an apostrophe (`Lina's_Welcome.png`) — resolved by converting to a Windows-style path via `cygpath -w` first; no workaround was needed for the file name itself, which was used exactly as supplied | Direct instruction: "Remove the background of Chef Lina." This session has no working background-removal capability (no Python/rembg, the Apprigate MCP `remove_background` tool is not connected) — the same limitation already surfaced and accepted earlier this session for an unrelated request — so the client was asked how to proceed and chose to supply an already background-removed file rather than have a crude fallback attempted | `npm run admin:check` clean, 90/90 unit tests, 51/51 rules tests, 0 vulnerabilities — unchanged from baseline. Re-verified in real headless Chromium at 1440/1280/768/390px: zero horizontal overflow, zero console errors, first screen still fits exactly within `100svh`/`100dvh` at every width with the new image in place. Screenshots: `assets/mockups/working/client-review-poster-led/v2-*.png` (overwritten in place with the cutout version) | Claude | **Preview only, as instructed. No commit, push, merge or deploy. `Lina's_Welcome.png`'s provenance/usage rights fall under the same open Client Inputs Register item as the rest of the 2026-08-02 batch (I-018) — not a new confirmation gap, since it was supplied directly by the client in this session, but recorded there for consistency** |

| D-028 | 2026-08-02 | **Homepage hero image swapped to a fully isolated chef cutout, and a real mobile clipping bug fixed.** The client supplied an updated `Lina's Welcome.png` (1800×2400, RGBA — a different, better export than D-027's file: this one has the trailer counter and signage removed too, a genuine full-body silhouette holding the plate, not a partial cutout). Verified alpha=0 at all four corners plus interior background samples before use (IHDR/IDAT decoded, not assumed). Produced `lina-welcome-hero-working-v1-20260802.webp` (1000×1333, `yuva420p`, ~102KB); `home-media.js`'s `heroImage` repointed at it, superseding D-027's `lina-hero-chef-cutout-working-v1-20260802.webp` in this one slot (that file and its `.vercelignore` line are removed since nothing references it now; the underlying source PNGs from both rounds stay on disk untouched). Also reverted an out-of-band edit to `index.html`'s hero `<img>` that had appeared hardcoded directly to the undeployed `assets/source/media/...` path (would 404 in production — that folder is deliberately never allowlisted) with alt text reading "Chef Lebo" (contradicts the confirmed public name, Chef Lina) — restored to the config-driven `id="homeHeroImage" src=""` pattern that `home.js` populates from `home-media.js`, per the standing single-source-of-truth rule for this page's media. **Real bug found and fixed while testing the 6 requested widths (1440/1280/1024/768/390/375):** at 375×667 and 390×844, the hero's `padding-top` (72px) was shorter than the header's actual rendered height (85px at <640px, 93px at 640–899px, since the logo grows at the 640px breakpoint) — the tagline/eyebrow were rendering behind the opaque fixed header, present in the DOM and computed `visibility:visible`/`opacity:~1` but invisible on screen. A second, compounding bug: `.home-hero__body`'s `align-items: center` on a single-column mobile grid, combined with `flex:1; min-height:0` on a container shorter than its content's natural height, clipped the top of `.home-hero__copy` off-screen instead of just letting it stack normally. Fixed by raising `padding-top` to 104px (covers both header heights with margin), changing `align-items` to `start` at mobile and `center` only at the ≥900px two-column breakpoint where it's actually needed, shrinking the mobile photo panel (240px → 190px, 150px under `max-height:700px`), and trimming supporting-paragraph spacing to reclaim vertical room. This is exactly the class of defect the earlier D-025/D-026/D-027 rounds' overflow checks never caught, because those checks only measured horizontal `scrollWidth` and the hero's own bottom edge against the viewport — which is tautologically always true for a `height:100svh` element and blind to internal clipping. This round's test script instead measures each key element's own `getBoundingClientRect()` against the header's bottom edge and the viewport bounds | Direct instruction: swap the hero image to the newly supplied file, keep it centrally configured (not hardcoded), keep the deployment allowlist correct, and explicitly test 6 named widths including two not tested in any prior round (1024, 375) — the additional widths are exactly what surfaced the clipping bug | `npm run admin:check` clean, 90/90 unit tests, 51/51 rules tests, 0 vulnerabilities — unchanged from baseline, re-confirmed after the CSS fix. Re-verified all 6 widths with per-element geometry checks (not just horizontal overflow): zero header/content overlap, zero top/bottom clipping on the tagline, hero image, ribbon or scroll cue, zero horizontal overflow, zero console errors, zero failed requests at any width. Screenshots: `assets/mockups/working/client-review-poster-led/v4-*.png` | Claude | **Preview only, as instructed. No commit, push, merge or deploy** |

| D-029 | 2026-08-03 | **Approved and produced the Lina's Interactive Digital Menu v1.0 REVIEW asset.** Tebogo approved the Phase 1 Change Contract, confirmed the ambiguous source-menu value as R12, confirmed the current phone/email/Instagram/address/hours/mobile-kitchen location, and approved the current poster-led website direction as the PDF's visual reference. The editable HTML/CSS/config source reads the existing 18-item `menu-data.js`; a zero-package Node exporter uses installed Chrome to create an 8-page A4 interactive PDF plus a per-link CSV manifest and QA report. Individual meal actions use pre-filled WhatsApp messages rather than unsupported cart deep links; catering actions use the production `/contact` journey. | The existing supplied PDF was a static one-page menu with no links or repeatable generation source. This asset connects verified menu content to the two actual customer journeys without changing the live cart, API, backend, routes, packages or deployment. | Verified: 18/18 items and prices reconcile; 61/61 visible PDF actions pass endpoint/protocol validation; 14/14 internal destinations exist; fonts are embedded with Unicode maps; selectable text and A4 dimensions confirmed; 1.47 MiB file; all eight pages visually reviewed at 100%, 150%, 150 DPI and representative 390px phone-fit width; no website files changed. Production `/menu` and `/contact` remain behind the existing Coming Soon gate, so the filename/status stays REVIEW and no deployment occurred. | Codex | **Review asset complete; awaiting client approval and a separate Coming Soon release decision before customer distribution** |

| D-030 | 2026-08-13 | **Lebo (`lebo@linas.co.za`) created as the real Owner admin account in live Production; Tebogo's existing Owner claim downgraded to Developer**, resolving the account-audit hold recorded in Client Inputs Register I-016. Ran the existing controlled bootstrap mechanism (`scripts/firebase-admin-bootstrap.js --email lebo@linas.co.za --role owner --name "Lebo" --confirm`) against real Production Firebase: created a new Firebase Auth user, set the `role: "owner"` custom claim, wrote `adminUsers/{uid}` (`active: true`), and generated a one-time password-*set* link (not a password — nothing was invented or emailed automatically; the link was returned for a human to relay to Lebo directly). Tebogo's account (existing Auth user, previously `role: "owner"` in both the custom claim and `adminUsers` doc) was then changed to `role: "developer"` via a small one-off script mirroring the same Admin SDK calls, since the shared bootstrap CLI's argument validation only accepts `owner`/`staff` and predates this project's later `developer`/`observer` roles (D-020); the temporary script was deleted immediately after the one confirmed run, per this project's established temporary-script convention | Direct explicit instruction, given after I surfaced I-016's standing hold and confirmed with two questions: (1) proceed against live Production now, and (2) whether to also downgrade Tebogo's claim in the same action — both confirmed | Pre-change state verified first (Tebogo: `role:"owner"` in both Auth claim and Firestore doc; Lebo: no existing Auth user — clean creation, no conflict). Post-change state verified by re-reading both records back from Firebase: Lebo's `adminUsers` doc shows `role:"owner"`, `active:true`; Tebogo's Auth claim and Firestore doc both confirmed `role:"developer"`. No other `adminUsers` record touched. Rebecca (Observer, `rlegodi@gep.co.za`) remains not yet invited — out of scope for this instruction | Claude | **Live in Production. Password-set link for Lebo was returned in-session for the operator to relay directly (not sent by any automated channel) — it expires and is single-use, so it must be shared with Lebo promptly. Tebogo will need to re-authenticate to pick up the new `developer` claim on next sign-in (existing session tokens carry the old `owner` claim until refreshed/expired). I-016 updated to reflect resolution.** |

## New entry template
| D-___ | YYYY-MM-DD | [decision] | [reason] | [scope/time/technical impact] | [name] | Proposed/Approved/Reversed |
